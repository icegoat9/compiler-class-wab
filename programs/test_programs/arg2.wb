// arg2.c
// test a feature to allow passing of command-line arguments
//  (which would set globals argc, arg1, arg2)

// code below should be wrapped in a mainuser(argc,arg1,arg2) { [user code] }
//  function by a compiler pass.

print argc;
if argc > 0 {
    print arg1;
    if argc > 1 {
        print arg2;
    }
}


