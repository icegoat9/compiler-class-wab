// program to test for structures when index variable already exists
// (implicit generation of a second variable declaration by the for loop may cause error)

var i = 5;
print i;

for i=1;i<=10;i=i+1; {
    print i;
}

print i;
