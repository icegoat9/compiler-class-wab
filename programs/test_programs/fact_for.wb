// fact_for.wb
// factorial, using (newer) for function to shorten code

func fact(n) {
    var result = 1;
    for x = 1,n {
        result = result * x;
    }
    return result;
}

for x = 1,9 {
    print fact(x);
}
